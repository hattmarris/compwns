Transpile components to dist directory

```
yarn run build
```

Run storybook on port 9001 locally

```
yarn run storybook
```

Build storybook as static site locally

```
yarn run build:storybook
```
